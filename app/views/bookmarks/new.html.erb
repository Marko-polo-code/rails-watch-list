<h1>New Bookmark</h1>

<%= form_with(model: [@list, @bookmark]) do |form| %>
  <div>
    <%= form.label :comment %>
    <%= form.text_field :comment %>
  </div>

  <div>
    <%= form.label :movie_id, 'Movie' %>
    <%= form.collection_select :movie_id, Movie.all, :id, :title %>
  </div>

  <%= form.submit 'Create Bookmark' %>
<% end %>

<a><%= link_to "My List", list_path(@list) %></a>
